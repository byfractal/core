---
description: When installing shadcn or adding shadcn components
globs: 
alwaysApply: false
---

## **Objective**  
Ensure users use the **updated ShadCN installation command** and are aware of upcoming deprecations.  

## **Rules & Guidelines**  
1. **Command Update Enforcement:**  
   - If the user attempts to install **ShadCN using an outdated command**, correct it automatically:  
     ```bash
     npx install shadcn-ui@latest → npx shadcn@latest init
     ```
   
2. **Deprecation Warning:**  
   - Display a **warning message** if the user references `shadcn-ui`:  
     ```
     [!WARNING] The `npx shadcn-ui` CLI is going to be deprecated soon.
     Please use: `npx install shadcn`.
     ```

3. **Chat Response Formatting:**  
   - Prefix responses with **(🚀rule:install-shadcn)** to indicate **automated enforcement** of this rule.  

## **Behavior Summary**  
- Ensure users **follow the correct installation method**.  
- Warn them **about the CLI deprecation**.  
- Maintain consistency by **enforcing the new installation process**.  

---

🚀 **This rule ensures users transition smoothly to the new ShadCN installation workflow while maintaining clarity in setup instructions.**  